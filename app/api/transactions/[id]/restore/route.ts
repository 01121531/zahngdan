import { requireAuth, verifySameOrigin } from '@/lib/auth'; import { ensureDatabase, getD1 } from '@/lib/database'; import { jsonError } from '@/lib/http';
export async function POST(request: Request, context: { params: Promise<{ id: string }> }) { const unauthorized = await requireAuth(request); if (unauthorized) return unauthorized; if (!verifySameOrigin(request)) return jsonError('请求来源不可信', 403); await ensureDatabase(); const { id } = await context.params; const cutoff = new Date(Date.now() - 30 * 86400000).toISOString(); const result = await getD1().prepare('UPDATE transactions SET deleted_at = NULL, updated_at = ? WHERE id = ? AND deleted_at > ?').bind(new Date().toISOString(), id, cutoff).run(); if (!result.meta.changes) return jsonError('账单已超过可恢复期限或不存在', 410); return Response.json({ ok: true }); }
