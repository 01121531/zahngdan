import { requireAuth, verifySameOrigin } from '@/lib/auth'; import { ensureDatabase, getD1, getFiles } from '@/lib/database'; import { jsonError } from '@/lib/http';
export async function DELETE(request: Request, context: { params: Promise<{ id: string }> }) { const unauthorized = await requireAuth(request); if (unauthorized) return unauthorized; if (!verifySameOrigin(request)) return jsonError('请求来源不可信', 403); await ensureDatabase(); const { id } = await context.params; const file = await getD1().prepare('SELECT object_key AS objectKey, preview_object_key AS previewObjectKey FROM attachments WHERE id = ? AND deleted_at IS NOT NULL').bind(id).first<{ objectKey: string; previewObjectKey: string | null }>(); if (!file) return jsonError('回收站中没有这个附件', 404); await getFiles().delete(file.objectKey); if (file.previewObjectKey) await getFiles().delete(file.previewObjectKey); await getD1().prepare('DELETE FROM attachments WHERE id = ?').bind(id).run(); return Response.json({ ok: true }); }
