import { cn } from '@/lib/cn';
export function Field({ label, error, hint, children, className }: { label: string; error?: string; hint?: string; children: React.ReactNode; className?: string }) { return <label className={cn('block', className)}><span className="mb-2 block text-sm font-semibold">{label}</span>{children}{hint && !error ? <span className="mt-1.5 block text-xs text-[var(--muted)]">{hint}</span> : null}{error ? <span role="alert" className="mt-1.5 block text-xs font-medium text-[var(--danger)]">{error}</span> : null}</label>; }
export const inputClass = 'min-h-12 w-full rounded-xl border border-[var(--line)] bg-[var(--surface)] px-3.5 text-base text-[var(--ink)] outline-none transition placeholder:text-[var(--muted)] focus:border-[var(--brand)] focus:ring-2 focus:ring-[var(--brand-soft)]';
